export async function embedText(input:string):Promise<number[]|null>{const key=process.env.OPENAI_API_KEY;if(!key||!input.trim())return null;const r=await fetch("https://api.openai.com/v1/embeddings",{method:"POST",headers:{Authorization:"Bearer "+key,"Content-Type":"application/json"},body:JSON.stringify({model:process.env.OPENAI_EMBEDDING_MODEL||"text-embedding-3-small",input:input.slice(0,12000)})});if(!r.ok)return null;const j=await r.json(),v=j.data?.[0]?.embedding;return Array.isArray(v)?v.map(Number):null}