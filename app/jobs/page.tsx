import Link from "next/link";import {demoJobs} from "../../lib/demo";export default function JobsPage(){return <main className="container section"><Link href="/">← Home</Link><p className="eyebrow">Opportunity index</p><h1>Latest jobs & internships</h1><p>Fresh listings with transparent source and key application details.</p><div className="grid">{demoJobs.concat(demoJobs).map((j,i)=><article className="card" key={j.id+i}><div className="eyebrow">{j.posted}</div><h2 className="title">{j.title}</h2><div className="company">{j.company}</div><div className="meta"><span className="badge">{j.type}</span><span className="badge">{j.location}</span></div><p>{j.salary} · {j.source}</p><Link href={"/jobs/"+j.id}>Open listing →</Link></article>)}</div></main>}