import {notFound} from "next/navigation";import Link from "next/link";import {demoJobs} from "../../../lib/demo";export async function generateStaticParams(){return demoJobs.map(j=>({slug:j.id}))}export default async function JobPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const job=demoJobs.find(j=>j.id===slug);if(!job)notFound();const schema={"@context":"https://schema.org","@type":"JobPosting","title":job!.title,"description":"Opportunity details should be verified on the original source.","datePosted":new Date().toISOString(),"employmentType":job!.type==="Internship"?"INTERN":"FULL_TIME","hiringOrganization":{"@type":"Organization","name":job!.company},"jobLocation":{"@type":"Place","address":{"@type":"PostalAddress","addressLocality":job!.location}}};return <main className="container section"><Link href="/jobs">← All opportunities</Link><article className="card" style={{marginTop:20,maxWidth:820}}><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><div className="eyebrow">{job!.posted} · {job!.type}</div><h1>{job!.title}</h1><p className="company">{job!.company}</p><div className="facts"><div className="fact"><b>Location</b>{job!.location}</div><div className="fact"><b>Compensation</b>{job!.salary}</div><div className="fact"><b>Source</b>{job!.source}</div><div className="fact"><b>Status</b>Verify before applying</div></div><h2>About this opportunity</h2><p>This canonical detail page preserves source attribution. In production, the ingestion pipeline will populate employer-provided responsibilities, qualifications, skills, deadline and source URL; AI will normalize and classify rather than invent facts.</p><a className="btn" href="#" rel="nofollow">Apply on source →</a></article></main>}