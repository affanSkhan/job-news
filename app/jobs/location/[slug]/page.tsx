import type {Metadata} from "next";
import {SITE_URL} from "../../../lib/site";
import Link from "next/link";import {notFound} from "next/navigation";import {getActiveJobsAsync,slugify} from "../../../../lib/jobs";export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const label=slug.split("-").map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(" ");return{title:`Jobs in ${label} | RolePilot`,description:`Current source-backed jobs in ${label}, including direct application links.`,alternates:{canonical:`/jobs/location/${slug}`}}}export default async function LocationPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,jobs=(await getActiveJobsAsync()).filter(j=>slugify(j.location)===slug);if(jobs.length<3)notFound();const label=slug.split("-").map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(" ");return <main className="container section"><p className="eyebrow">Location jobs</p><h1>Jobs in {label}</h1><p>{jobs.length} current source-backed opportunities.</p><div className="grid">{jobs.slice(0,120).map(j=><article className="card" key={j.id}><div className="eyebrow">{j.freshness} · {j.sourceName}</div><h2 className="title"><Link href={"/jobs/"+j.slug}>{j.title}</Link></h2><div className="company">{j.company}</div><p>{j.type} · {j.workMode} · {j.salary}</p></article>)}</div></main>}