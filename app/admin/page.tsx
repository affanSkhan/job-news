"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function Admin(){const [d,setD]=useState<any>(null),[msg,setMsg]=useState("");async function load(){const r=await fetch("/api/admin/overview");if(r.status===403){setMsg("Admin access required.");return}setD(await r.json())}useEffect(()=>{load()},[]);async function toggle(id:string,enabled:boolean){await fetch("/api/admin/sources",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,enabled})});load()}return <main className="container section"><nav><Link href="/">← JobNews</Link></nav><p className="eyebrow">Control plane</p><h1>JobNews Admin</h1>{msg&&!d?<div className="empty"><p>{msg}</p></div>:!d?<p>Loading health…</p>:<><div className="grid"><div className="card"><div className="eyebrow">Active jobs</div><h2>{d.jobs}</h2></div><div className="card"><div className="eyebrow">Companies</div><h2>{d.companies}</h2></div><div className="card"><div className="eyebrow">Enabled sources</div><h2>{d.sources}</h2></div><div className="card"><div className="eyebrow">Instrumentation</div><p>GA: {d.analyticsConfigured?"configured":"missing"} · AdSense: {d.adsenseConfigured?"configured":"missing"}</p></div></div><section className="card"><h2>Source health</h2><div className="grid">{d.sourceRows.map((s:any)=><article className="card" key={s.id}><h3>{s.name}</h3><p>{s.kind} · {s.last_count||0} jobs</p><p className="score">{s.last_error||("Last success: "+(s.last_success_at||"never"))}</p><button className="chip" onClick={()=>toggle(s.id,!s.enabled)}>{s.enabled?"Disable":"Enable"}</button></article>)}</div></section><section className="card"><h2>Ingestion history</h2><ul>{d.runs.map((r:any)=><li key={r.id}>{new Date(r.started_at).toLocaleString()} · {r.status} · {r.jobs_upserted} published · {r.sources_succeeded}/{r.sources_total} sources OK</li>)}</ul></section></>}</main>}