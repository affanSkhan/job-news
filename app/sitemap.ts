import type {MetadataRoute} from "next";import {demoJobs} from "../lib/demo";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://job-news.vercel.app";return [{url:base,lastModified:new Date(),priority:1},{url:base+"/jobs",lastModified:new Date(),priority:.9},{url:base+"/about",lastModified:new Date(),priority:.5},...demoJobs.map(j=>({url:base+"/jobs/"+j.id,lastModified:new Date(),priority:.7}))]}