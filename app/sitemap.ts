import type {MetadataRoute} from "next";import {getActiveJobsAsync,slugify,companySlug} from "../lib/jobs";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://job-news-prod.onrender.com",jobs=await getActiveJobsAsync(),companies=new Set<string>(),skills=new Set<string>(),locations=new Set<string>(),categories=new Set<string>();for(const j of jobs){if(j.company)companies.add(companySlug(j.company));for(const s of j.skills)skills.add(slugify(s));if(j.location)locations.add(slugify(j.location));if(j.category)categories.add(slugify(j.category))}return[{url:base,lastModified:new Date(),priority:1},{url:base+"/jobs",lastModified:new Date(),priority:.95},{url:base+"/companies",lastModified:new Date(),priority:.85},{url:base+"/skills",lastModified:new Date(),priority:.8},{url:base+"/locations",lastModified:new Date(),priority:.8},{url:base+"/about",lastModified:new Date(),priority:.5},{url:base+"/privacy",lastModified:new Date(),priority:.2},{url:base+"/disclaimer",lastModified:new Date(),priority:.2},...([...companies] as string[]).map(x=>({url:base+"/companies/"+x,lastModified:new Date(),priority:.65})),...([...skills] as string[]).map(x=>({url:base+"/skills/"+x,lastModified:new Date(),priority:.6})),...([...locations] as string[]).map(x=>({url:base+"/locations/"+x,lastModified:new Date(),priority:.6})),...jobs.map(j=>({url:base+"/jobs/"+j.slug,lastModified:new Date(j.updatedAt||j.publishedAt),priority:.8}))]}