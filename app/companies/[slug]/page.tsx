import {notFound} from "next/navigation";import Link from "next/link";import {getActiveJobsAsync,companySlug} from "../../../lib/jobs";import type {Metadata} from "next";export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,jobs=await getActiveJobsAsync(),j=jobs.find(x=>companySlug(x.company)===slug);return j?{title:j.company+" Jobs",description:"Current jobs and internships from "+j.company+" on RolePilot.",alternates:{canonical:"/companies/"+slug}}:{}}
export default async function Company({params}:{params:Promise<{slug:string}>}){const {slug}=await params,jobs=(await getActiveJobsAsync()).filter(j=>companySlug(j.company)===slug);if(jobs.length<2)notFound();const company=jobs[0].company;const sources=[...new Set(jobs.map(j=>j.sourceName))],locs=[...new Set(jobs.map(j=>j.location))];const schema={"@context":"https://schema.org","@type":"Organization","name":company};return <main className="container section"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><p className="eyebrow">Company intelligence</p><h1>{company} jobs</h1><p>{jobs.length} active openings across {locs.length} location groups and {sources.length} public sources.</p><div className="meta">{sources.map(s=><span className="badge" key={s}>{s}</span>)}</div><div className="grid">{jobs.slice(0,120).map(j=><article className="card" key={j.id}><div className="eyebrow">{j.freshness}</div><h2 className="title"><Link href={"/jobs/"+j.slug}>{j.title}</Link></h2><p>{j.location} · {j.workMode} · {j.salary}</p></article>)}</div></main>}