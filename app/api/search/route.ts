import {NextResponse} from "next/server";import {semanticSearch} from "../../../lib/search";export async function GET(req:Request){const q=new URL(req.url).searchParams.get("q")?.trim()||"";if(!q)return NextResponse.json({items:[]});const results=await semanticSearch(q);return NextResponse.json({items:results.map(x=>({...x.job,score:x.score})),semantic:true})}