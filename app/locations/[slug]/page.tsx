import Link from "next/link";import {notFound} from "next/navigation";import {getActiveJobsAsync,slugify} from "../../../lib/jobs";export const dynamic="force-dynamic";export default async function Location({params}:{params:Promise<{slug:string}>}){const {slug}=await params,jobs=(await getActiveJobsAsync()).filter(j=>slugify(j.location)===slug);if(jobs.length<3)notFound();return <main className="container section"><p className="eyebrow">Location jobs</p><h1>Jobs in {slug.replace(/-/g," ")}</h1><p>{jobs.length} active listings with direct source links.</p><div className="grid">{jobs.slice(0,120).map(j=><article className="card" key={j.id}><h2 className="title"><Link href={"/jobs/"+j.slug}>{j.title}</Link></h2><p>{j.company} · {j.workMode} · {j.salary}</p></article>)}</div></main>}