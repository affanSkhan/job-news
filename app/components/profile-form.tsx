"use client";import {useState} from "react";export default function ProfileForm({profile}:{profile:any}){const [p,setP]=useState({full_name:profile?.full_name||"",headline:profile?.headline||"",desired_titles:(profile?.desired_titles||[]).join(", "),skills:(profile?.skills||[]).join(", "),preferred_locations:(profile?.preferred_locations||[]).join(", "),preferred_work_modes:(profile?.preferred_work_modes||[]).join(", "),experience_level:profile?.experience_level||"",profile_text:profile?.profile_text||""}),[msg,setMsg]=useState("");
const save=async(e:React.FormEvent)=>{e.preventDefault();setMsg("Saving…");const body={...p,desired_titles:p.desired_titles.split(",").map((x:string)=>x.trim()).filter(Boolean),skills:p.skills.split(",").map((x:string)=>x.trim()).filter(Boolean),preferred_locations:p.preferred_locations.split(",").map((x:string)=>x.trim()).filter(Boolean),preferred_work_modes:p.preferred_work_modes.split(",").map((x:string)=>x.trim()).filter(Boolean)};const r=await fetch("/api/profile",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(body)});setMsg(r.ok?"Saved. Radar matching is queued for the next index refresh.":"Could not save your profile.")};
return <form onSubmit={save} style={{display:"grid",gap:10}}><input className="input" placeholder="Full name" value={p.full_name} onChange={e=>setP({...p,full_name:e.target.value})}/><input className="input" placeholder="Headline" value={p.headline} onChange={e=>setP({...p,headline:e.target.value})}/><input className="input" placeholder="Target roles, comma separated" value={p.desired_titles} onChange={e=>setP({...p,desired_titles:e.target.value})}/><input className="input" placeholder="Skills, comma separated" value={p.skills} onChange={e=>setP({...p,skills:e.target.value})}/><input className="input" placeholder="Preferred locations, comma separated" value={p.preferred_locations} onChange={e=>setP({...p,preferred_locations:e.target.value})}/><input className="input" placeholder="Work modes: remote, hybrid, onsite" value={p.preferred_work_modes} onChange={e=>setP({...p,preferred_work_modes:e.target.value})}/><input className="input" placeholder="Experience level" value={p.experience_level} onChange={e=>setP({...p,experience_level:e.target.value})}/><textarea className="input" style={{height:130,paddingTop:12}} placeholder="Describe your ideal role, stack and constraints for semantic matching" value={p.profile_text} onChange={e=>setP({...p,profile_text:e.target.value})}/><button className="btn">Save profile</button>{msg&&<span className="score">{msg}</span>}</form>