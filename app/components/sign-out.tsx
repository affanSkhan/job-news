"use client";import {authClient} from "../../lib/auth/client";export default function SignOut(){return <button className="chip" onClick={async()=>{await authClient.signOut();location.href="/"}}>Sign out</button>}