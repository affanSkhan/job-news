"use client";import {useState} from "react";export default function SaveJob({jobId}:{jobId:string}){const [msg,setMsg]=useState("");async function click(){const r=await fetch("/api/saved-jobs",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({jobId})});if(r.status===401){setMsg("Sign in to save");location.href="/auth?next="+encodeURIComponent(location.pathname);return}setMsg(r.ok?"Saved":"Could not save")}return <button className="chip" onClick={click}>☆ Save job {msg&&" · "+msg}</button>}