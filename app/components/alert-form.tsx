"use client";import {useState} from "react";export default function AlertForm(){const [name,setName]=useState("My job alert"),[q,setQ]=useState(""),[mode,setMode]=useState(""),[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/alerts",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,criteria:{q,mode},frequency:"daily"})});setMsg(r.ok?"Alert saved.":"Could not save alert.")}return <form onSubmit={submit} style={{display:"grid",gap:10}}><input className="input" value={name} onChange={e=>setName(e.target.value)} placeholder="Alert name"/><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="AI engineer, Python, Pune…"/><select className="select" value={mode} onChange={e=>setMode(e.target.value)}><option value="">Any work mode</option><option value="remote">Remote</option><option value="hybrid">Hybrid</option><option value="onsite">On-site</option></select><button className="btn">Create daily alert</button>{msg&&<span className="score">{msg}</span>}</form>}