"use client";import {useState} from "react";export default function StatusSelect({jobId,status}:{jobId:string;status:string}){const [value,setValue]=useState(status),[msg,setMsg]=useState("");async function change(v:string){setValue(v);const r=await fetch("/api/applications",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({jobId,status:v})});setMsg(r.ok?"":"!")}return <span><select className="select" value={value} onChange={e=>change(e.target.value)} style={{height:38,minWidth:130}}><option value="saved">Saved</option><option value="applied">Applied</option><option value="interview">Interview</option><option value="offer">Offer</option><option value="rejected">Rejected</option><option value="withdrawn">Withdrawn</option></select>{msg&&<span className="score">{msg}</span>}</span>}