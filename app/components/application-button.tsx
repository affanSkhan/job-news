"use client";import {useState} from "react";export default function ApplicationButton({jobId}:{jobId:string}){const [msg,setMsg]=useState("");async function apply(){const r=await fetch("/api/applications",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({jobId,status:"applied"})});if(r.status===401){location.href="/auth?next="+encodeURIComponent(location.pathname);return}setMsg(r.ok?"Tracked as applied":"Could not update")}return <button className="chip" onClick={apply}>✓ Mark applied{msg&&" · "+msg}</button>}