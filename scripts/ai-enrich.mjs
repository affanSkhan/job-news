import fs from "node:fs/promises";import {neon} from "@neondatabase/serverless";
const hf=process.env.HF_TOKEN,openai=process.env.OPENAI_API_KEY,db=process.env.DATABASE_URL?neon(process.env.DATABASE_URL):null;if(!hf&&!openai){console.log("No AI credential configured; enrichment safely skipped.");process.exit(0)}
const jobs=JSON.parse(await fs.readFile("data/jobs.json","utf8")),targets=jobs.filter(j=>!j.aiSummary).slice(0,80),hfModel=process.env.HF_MODEL||"Qwen/Qwen3-0.6B:fastest",openaiModel=process.env.OPENAI_CHAT_MODEL||"gpt-4o-mini";
async function enrich(job){const prompt=["Return strict JSON only with keys summary and highlights.","Do not invent facts. Use only supplied source text.","summary <= 35 words; highlights <= 4 short factual strings.","TITLE: "+job.title,"COMPANY: "+job.company,"LOCATION: "+job.location,"DESCRIPTION: "+job.description.slice(0,8000)].join("\n");if(hf){const r=await fetch("https://router.huggingface.co/v1/chat/completions",{method:"POST",headers:{Authorization:"Bearer "+hf,"Content-Type":"application/json"},body:JSON.stringify({model:hfModel,messages:[{role:"user",content:prompt}],temperature:.1,max_tokens:220})});if(r.ok){const j=await r.json(),t=j.choices?.[0]?.message?.content||"",m=t.match(/\{[\s\S]*\}/);if(m)try{return JSON.parse(m[0])}catch{}}}if(openai){const r=await fetch("https://api.openai.com/v1/chat/completions",{method:"POST",headers:{Authorization:"Bearer "+openai,"Content-Type":"application/json"},body:JSON.stringify({model:openaiModel,messages:[{role:"system",content:"Summarize job listings without inventing facts. Return JSON with summary and highlights."},{role:"user",content:prompt}],temperature:.1,response_format:{type:"json_object"}})});if(r.ok){const j=await r.json(),t=j.choices?.[0]?.message?.content||"{}";try{return JSON.parse(t)}catch{}}}return null}
for(const job of targets)try{const x=await enrich(job);if(x){job.aiSummary=String(x.summary||"").slice(0,500);job.aiHighlights=Array.isArray(x.highlights)?x.highlights.map(String).slice(0,4):[]}}catch(e){console.error("AI failed",job.id,e?.message||String(e))}
await fs.writeFile("data/jobs.json",JSON.stringify(jobs,null,2)+"\n");
if(db){for(const job of targets.filter(j=>j.aiSummary))await db.query("UPDATE public.jobs SET ai_summary=$1,ai_highlights=$2,updated_at=now() WHERE id=$3",[job.aiSummary,job.aiHighlights||[],job.id])}
console.log("AI-enriched",targets.filter(j=>j.aiSummary).length,"jobs");